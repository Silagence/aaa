# 文字冒险游戏（AVG）在线编辑器网站 需求文档

> 版本：v1.0
> 日期：2026-09-11
> 状态：初稿

---

## 1. 项目概述

### 1.1 项目背景
本项目是一个基于 Web 的文字冒险游戏（AVG / Visual Novel）在线编辑器网站，让创作者无需安装客户端、无需编写代码，即可在浏览器中编写剧本、调用平台内置静态资源生成场景、产出一份标准化的场景配置文件，并能直接预览/播放。后期扩展用户系统与作品分享，形成"创作 → 预览 → 发布 → 分享"的闭环。

### 1.2 项目目标
| 阶段 | 目标 |
|---|---|
| 一期（MVP） | 完成首页、编辑器、播放器三大核心页面，支持剧本编写、场景配置、配置文件导入导出、预览播放 |
| 二期 | 用户注册登录、用户信息管理、作品云端保存 |
| 三期 | 作品发布与分享页面、公开作品列表、作品嵌入与外链分享 |

### 1.3 参考项目
| 项目 | 借鉴点 |
|---|---|
| [ToAVGAcme](https://toavg.jushistudio.com/) | 零代码可视化创作、剧情画布、卡片式属性编辑、演出系统、素材库管理 |
| [Tuesday JS](https://github.com/Kirilllive/tuesday-js) | 纯 Web 端可视化场景编辑器、场景布局、跨屏预览 |
| [WebGAL](https://github.com/OpenWebGAL/WebGAL) | 脚本命令体系（changeBg/say/choose/setVar）、场景脚本文件结构 |
| [VNgine](https://github.com/ThijssjihT/VNgine) / [GinkgoEngine](https://github.com/1366sll/GinkgoEngine) | 场景 JSON 配置格式（scene + commands/nodes） |
| [StoryWeaver](https://github.com/ehewlett3/StoryWeaver.online) | PHP 部署形态、用户系统、分享与权限模型 |
| [Twine](https://twinery.org/) | 段落（Passage）+ 分支链接的剧情结构思想 |

---

## 2. 术语定义

| 术语 | 含义 |
|---|---|
| 剧本（Script） | 创作者编写的故事文本，是场景的来源 |
| 场景（Scene） | 剧本中一段可播放的单元，由背景、立绘、对话、选项、音频等元素组成 |
| 节点（Node） | 场景内的最小播放单位，如一句对话、一次背景切换、一个选项分支 |
| 配置文件（Config） | 编辑器产出的标准化文件（JSON 或 TXT），描述全部场景与播放逻辑 |
| 素材（Asset） | 平台内置的背景、立绘、BGM、音效、字体等静态资源 |
| 作品（Work） | 一份完整配置 + 元信息（标题、作者、封面等）构成的可在播放器运行的整体 |
| 演出（Performance） | 节点在播放器中的呈现方式，含打字机、转场、动画等 |

---

## 3. 用户角色

| 角色 | 说明 | 一期 | 二期 | 三期 |
|---|---|---|---|---|
| 访客 | 未登录用户 | ✅ | ✅ | ✅ |
| 创作者 | 已注册并创作内容的用户 | — | ✅ | ✅ |
| 玩家 | 浏览/播放他人分享作品的用户 | — | — | ✅ |
| 管理员 | 管理用户、作品、素材 | — | ✅ | ✅ |

---

## 4. 功能需求

### 4.1 首页（Home）

**路由：** `/` 或 `index.php`

**功能点：**
1. 展示项目主背景图/动效（建议 1920×1080，支持移动端自适应）。
2. 展示项目主标题与副标题文案。
3. 提供"开始使用"主按钮，点击后跳转至编辑器页面 `/editor`。
4. 提供二级入口：
   - "作品广场"（三期上线，一期置灰）→ `/works`
   - "登录/注册"（二期上线，一期置灰）→ `/login`
5. 页脚展示版权、备案号、联系方式占位。

**交互：**
- 首屏加载性能 ≤ 2s（首屏可见）。
- 按钮具备 hover/active 反馈。
- 移动端响应式：标题与按钮垂直堆叠。

---

### 4.2 编辑器页面（Editor）

**路由：** `/editor`

#### 4.2.1 整体布局（参考 ToAVGAcme / Tuesday JS）
采用三栏可折叠布局：
- **左栏：素材库 / 场景列表**
- **中栏：剧本编辑区 + 实时预览**
- **右栏：节点属性卡片**

#### 4.2.2 剧本编辑区
1. **文档式剧本书写**：以纯文本/快捷语法书写剧本，输入即创作。
2. **快捷插入**：工具栏一键插入"对话""选项""背景切换""立绘""BGM""变量"等节点。
3. **剧情画布（可选）**：以缩放地图形式展示前后顺序与分支走向（一期可降级为大纲列表）。
4. **撤销/重做**：支持 ≥ 50 步回退。
5. **自动保存**：草稿定时写入 `localStorage`，登录后同步至服务端（二期）。

#### 4.2.3 场景配置
创作者编写剧本时，调用平台内置静态资源为每个场景配置以下属性：

| 属性 | 说明 | 示例 |
|---|---|---|
| `sceneId` | 场景唯一标识 | `scene_001` |
| `background` | 背景图（来自素材库） | `bg/classroom_day.jpg` |
| `characters` | 立绘列表（角色 + 表情 + 位置） | `[{id:"alice", sprite:"alice_smile", pos:"left"}]` |
| `bgm` | 背景音乐 | `bgm/quiet_theme.mp3` |
| `sfx` | 音效 | `sfx/door.wav` |
| `speaker` | 当前说话角色名 | `爱丽丝` |
| `text` | 对话文本 | `欢迎来到教室。` |
| `voice` | 语音文件（可选） | `voice/alice_001.mp3` |
| `choices` | 选项分支 | `[{text:"进教室", next:"scene_002"}, {text:"离开", next:"scene_003"}]` |
| `next` | 默认下一场景 | `scene_002` |
| `effects` | 转场/特效 | `["fade"]` |
| `variables` | 变量读写 | `{set:{affection:+1}}` |

#### 4.2.4 素材库（内置静态资源）
1. 平台预置分类：背景、立绘（按角色 + 表情）、BGM、音效、字体。
2. 每项含：缩略图、文件名、分类标签、分辨率/时长元信息。
3. 支持网格/列表双视图，点击或拖拽插入到剧本。
4. 一期仅使用内置资源；二期可支持本地上传（受用户配额限制）。

#### 4.2.5 配置文件生成与导入导出
1. 编辑器将所有场景整理为一份 **JSON 配置文件**（详见第 6 节格式规范）。
2. **导出**：点击"导出配置"按钮，下载 `<作品名>.json`（含 `manifest` + `scenes`）。
3. **导入**：点击"导入配置"按钮，上传 JSON/TXT 文件，校验格式后还原至编辑器。
4. **校验**：导入时检查必填字段、引用的素材是否存在、`next/choices` 跳转目标是否存在；不通过则给出错误清单。
5. 导出格式同时提供 **TXT 脚本格式**（参考 WebGAL）作为可读备份，二者可互转。

#### 4.2.6 预览跳转
- 编辑器顶部"预览"按钮 → 新窗口/新路由打开播放器 `/player?mode=preview&work=<临时ID>`。
- 预览模式不要求登录，配置通过 `localStorage` 或 URL 参数传递，避免一期必须落库。

---

### 4.3 播放器页面（Player）

**路由：** `/player`

#### 4.3.1 播放流程
1. 读取配置文件（一期：从 URL 参数 / localStorage；二期：从服务端 `works` 表）。
2. 按 `scenes` 顺序逐节点渲染：
   - 加载背景 → 渲染立绘 → 播放 BGM → 打字机显示对话文本 → 等待用户点击。
   - 遇到 `choices` 节点：渲染选项按钮，等待用户选择后跳转 `next`。
   - 遇到 `variables` 节点：更新运行时变量状态。
3. 到达终点节点（无 `next` 且无 `choices`）显示"剧终"。

#### 4.3.2 播放器控制
| 控制 | 说明 |
|---|---|
| 点击/空格/→ | 推进到下一节点 |
| Ctrl（按住） | 快进（打字机瞬显，自动推进，遇选项停止） |
| Tab | 跳过至下一选项 |
| A | 自动播放（可设延迟） |
| L | 查看历史对话记录 |
| S | 存档（一期仅 localStorage） |
| O | 读档 |
| Esc | 设置面板（音量、语速） |

#### 4.3.3 演出效果
- 打字机文本（速度可调，点击瞬显）。
- 背景淡入淡出、立绘移动/缩放/淡入。
- 选项按钮悬停反馈。
- BGM 平滑过渡、音效独立通道。
- 一期至少实现：淡入淡出 + 打字机；其余可作为渐进增强项。

#### 4.3.4 存档与历史
- 一期：localStorage 存档槽 ≥ 3，含节点 ID 与截图缩略图。
- 一期：历史对话记录可滚动回看，已读可跳过。
- 二期：存档可同步至服务端（登录用户）。

---

### 4.4 二期功能：用户系统

#### 4.4.1 注册登录
- 路由：`/login`、`/register`
- 方式：邮箱 + 密码；可选手机号验证码。
- 密码使用 `password_hash` 存储，登录态使用 PHP `session` + 记住我 Cookie。
- 防刷：注册限频、登录失败计数锁定。

#### 4.4.2 用户信息管理
- 路由：`/profile`
- 字段：昵称、头像、简介、邮箱、注册时间、作品数。
- 操作：修改资料、修改密码、注销账号。
- 我的创作列表：展示该用户的所有作品，支持编辑/删除/发布/取消发布。

#### 4.4.3 作品云端保存
- 编辑器"保存"按钮将当前配置 + 元信息写入 `works` 表。
- 自动保存定时同步（每 60s 或失焦时）。
- 支持版本快照（可选，预留 `work_revisions` 表）。

---

### 4.5 三期功能：作品分享

#### 4.5.1 作品发布
- 创作者在 `/profile` 或编辑器内点击"发布"，将作品标记为公开。
- 发布时生成唯一分享短链 `/w/<short_code>` 与嵌入 iframe 代码。

#### 4.5.2 作品广场
- 路由：`/works`
- 列表分页展示公开作品：封面、标题、作者、简介、播放次数、点赞数。
- 筛选：最新、最热、按标签。
- 搜索：标题/作者关键字。

#### 4.5.3 作品详情与播放
- 路由：`/w/<short_code>`
- 展示作品信息 + "开始播放"按钮 → 进入 `/player?work=<work_id>`。
- 评论区（预留 `comments` 表）。
- 点赞、收藏、举报。

#### 4.5.4 嵌入分享
- 生成 iframe 嵌入代码，供外部站点引用。
- 分享至社交平台的卡片预览（OG 标签）。

### 4.6 三期功能：主题切换

#### 4.6.1 主题集合
- 提供 4 个主题：深色（默认）、浅色、护眼（低饱和暖色底）、跟随系统。
- 「跟随系统」通过 `prefers-color-scheme` 自动在深色/浅色间切换，并监听系统主题变化实时响应。

#### 4.6.2 切换入口与作用范围
- 入口：播放器设置面板（Esc）新增「主题」项，切换后立即生效。
- 作用范围：播放器、编辑器、首页三个页面统一跟随。
- 编辑器与首页不提供独立入口，仅读取并跟随播放器的主题设置。

#### 4.6.3 实现约束
- 主题变量集中定义于公共样式文件，三个页面共用，作为唯一的变量来源。
- 通过根元素 `data-theme` 属性切换，避免逐元素改样式。
- 清理各样式表中硬编码的强调色半透明值，统一改为变量，保证浅色主题下的对比度与可读性。
- 页面渲染前同步应用主题，避免主题闪烁（FOUC）。
- 主题偏好独立持久化，与播放器其他配置解耦。

---

## 5. 非功能需求

| 维度 | 要求 |
|---|---|
| 兼容性 | Chrome/Edge/Firefox/Safari 最新两个大版本；移动端 iOS/Android 主流浏览器 |
| 分辨率 | 桌面 ≥ 1280×720；移动端自适应 |
| 性能 | 首页 LCP ≤ 2.5s；编辑器加载 ≤ 3s；播放器节点切换 ≤ 200ms |
| 可用性 | 编辑器零代码上手；关键操作有 tooltip 与引导 |
| 安全 | 输入转义防 XSS；SQL 使用 PDO 预处理；CSRF Token；文件上传白名单 + 大小限制 |
| 可维护 | 前端模块化（一期可用原生 JS + 组件拆分，二期可引入构建工具）；后端 MVC 分层 |
| 国际化 | 文案集中管理，预留多语言键（一期中文） |
| 可访问性 | 键盘可操作；对比度满足 WCAG AA |

---

## 6. 配置文件格式规范（JSON）

### 6.1 顶层结构
```json
{
  "manifest": {
    "name": "示例作品",
    "author": "作者名",
    "version": "1.0.0",
    "created": "2026-09-11T10:00:00+08:00",
    "engine": "asha@1.0",
    "startScene": "scene_001",
    "canvas": { "width": 1920, "height": 1080, "orientation": "landscape" }
  },
  "assets": [
    { "type": "bg", "id": "classroom_day", "src": "bg/classroom_day.jpg" },
    { "type": "sprite", "id": "alice_smile", "src": "sprites/alice_smile.png" },
    { "type": "bgm", "id": "quiet_theme", "src": "bgm/quiet_theme.mp3" }
  ],
  "scenes": [
    { "id": "scene_001", "nodes": [ /* 见 6.2 */ ] }
  ]
}
```

### 6.2 节点（Node）示例
```json
[
  { "type": "bg", "ref": "classroom_day", "transition": "fade" },
  { "type": "bgm", "ref": "quiet_theme", "loop": true },
  { "type": "sprite", "ref": "alice_smile", "position": "left", "animation": "fadeIn" },
  { "type": "say", "speaker": "爱丽丝", "text": "欢迎来到教室。", "voice": "voice/alice_001.mp3" },
  { "type": "choose", "options": [
      { "text": "进教室", "next": "scene_002" },
      { "text": "离开", "next": "scene_003" }
  ]},
  { "type": "var", "set": { "affection": "+1" } },
  { "type": "goto", "next": "scene_002" }
]
```

### 6.3 节点类型枚举
| type | 必填字段 | 可选字段 |
|---|---|---|
| `bg` | `ref` | `transition`, `duration` |
| `sprite` | `ref` | `position`, `animation`, `duration` |
| `bgm` | `ref` | `loop`, `fade` |
| `sfx` | `ref` | — |
| `say` | `speaker`, `text` | `voice`, `speed`, `color` |
| `choose` | `options[]` | — |
| `var` | `set` 或 `if` | — |
| `goto` | `next` | — |

### 6.4 TXT 脚本格式（可读备份，参考 WebGAL）
```
#scene scene_001
bg:classroom_day;
bgm:quiet_theme;
sprite:alice_smile:left;
say:爱丽丝:欢迎来到教室。;
choose:进教室:scene_002 | 离开:scene_003;
```

---

## 7. 数据库设计（MySQL）

> 一期可不使用数据库（配置走文件/localStorage），二期起启用。下表为完整设计。

### 7.1 表结构概览
| 表 | 用途 |
|---|---|
| `users` | 用户账号 |
| `works` | 作品主表 |
| `work_revisions` | 作品历史版本（可选） |
| `work_assets` | 用户上传素材（二期） |
| `comments` | 作品评论 |
| `likes` | 点赞记录 |
| `sessions` | 会话/记住我令牌 |

### 7.2 关键字段
```sql
users(
  id BIGINT PK AI,
  email VARCHAR(190) UNIQUE,
  nickname VARCHAR(60),
  password_hash CHAR(60),       -- bcrypt
  avatar VARCHAR(255),
  bio TEXT,
  role ENUM('user','admin') DEFAULT 'user',
  created_at DATETIME,
  updated_at DATETIME
)

works(
  id BIGINT PK AI,
  user_id BIGINT FK,
  title VARCHAR(120),
  description VARCHAR(500),
  cover VARCHAR(255),
  config LONGTEXT,              -- 完整 JSON 配置
  short_code CHAR(8) UNIQUE,   -- 分享短链
  is_public TINYINT DEFAULT 0,
  play_count INT DEFAULT 0,
  like_count INT DEFAULT 0,
  created_at DATETIME,
  updated_at DATETIME,
  INDEX(user_id), INDEX(is_public)
)
```

---

## 8. 技术架构

### 8.1 技术选型
| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | PHP 8.x | 路由可自建 front controller 或使用轻量框架（如 Laravel / CodeIgniter） |
| 数据库 | MySQL 8.0 | PDO + 预处理 |
| 前端 | 原生 HTML/CSS/JS（一期）；可按需引入 Vite + 轻量框架（二期） | 编辑器与播放器为重交互页面 |
| 静态资源 | 平台内置目录 `/assets/...` | 一期只读 |
| 会话 | PHP session + 记住我 Cookie | 二期 |
| 部署 | Apache/Nginx + PHP-FPM | — |

### 8.2 目录结构建议
```
asha/
├── public/                # Web 根
│   ├── index.php          # 入口 / 首页
│   ├── editor.php         # 编辑器
│   ├── player.php         # 播放器
│   ├── login.php register.php profile.php   # 二期
│   ├── works.php w.php    # 三期
│   └── assets/            # 内置静态资源
│       ├── bg/ sprites/ bgm/ sfx/ fonts/
├── app/
│   ├── Controllers/
│   ├── Models/
│   ├── Services/          # 配置解析、校验、导出
│   └── DB.php
├── config/
├── storage/               # 用户上传/导出临时
└── sql/                   # 建表脚本
```

### 8.3 接口示意（二期起 REST 化）
| 方法 路径 | 说明 |
|---|---|
| POST `/api/works` | 保存作品 |
| GET `/api/works/{id}` | 读取作品 |
| POST `/api/works/{id}/publish` | 发布 |
| GET `/api/works?public=1` | 作品广场 |
| POST `/api/auth/login` | 登录 |
| POST `/api/auth/register` | 注册 |

---

## 9. 页面与路由总览

| 路由 | 页面 | 阶段 |
|---|---|---|
| `/` | 首页 | 一期 |
| `/editor` | 编辑器 | 一期 |
| `/player` | 播放器 | 一期 |
| `/login` `/register` | 登录注册 | 二期 |
| `/profile` | 用户信息管理 | 二期 |
| `/works` | 作品广场 | 三期 |
| `/w/{short_code}` | 作品分享/详情 | 三期 |

---

## 10. 里程碑与优先级

| 优先级 | 模块 | 内容 |
|---|---|---|
| P0 | 首页 | 背景标题 + 开始使用按钮 |
| P0 | 编辑器 | 剧本编写、场景配置、素材库调用、JSON 导入导出、预览跳转 |
| P0 | 播放器 | 按配置逐节点渲染、打字机、选项分支、存档(localStorage) |
| P1 | 编辑器增强 | 撤销重做、自动保存(localStorage)、剧情画布大纲 |
| P1 | 播放器增强 | 快进/跳过/自动播放、历史对话、设置面板 |
| P2 | 用户系统 | 注册登录、个人资料、我的创作 |
| P2 | 云端保存 | works 表、保存/读取/版本快照 |
| P3 | 作品分享 | 发布、短链、广场、详情、评论、点赞、嵌入分享 |
| P3 | 主题切换 | 深色/浅色/护眼/跟随系统，播放器设置面板切换，三页面统一跟随 |

---

## 11. 风险与约束

1. **静态资源体积**：内置素材库若过大将拖慢首屏，需做按需加载与缩略图。
2. **配置文件兼容**：JSON 格式需版本化（`manifest.version`），后续升级需向后兼容策略。
3. **PHP 单机部署**：一期不考虑分布式，但接口层应预留以便二期上云。
4. **XSS 风险**：剧本文本与评论均需服务端/前端双重转义。
5. **移动端编辑体验**：编辑器在小屏上体验有限，一期可声明"建议桌面端使用"。

---

## 12. 附录：术语与命令速查（参考 WebGAL）

| 命令 | 功能 | 示例 |
|---|---|---|
| `bg` | 切换背景 | `bg:classroom_day;` |
| `sprite` | 显示/隐藏立绘 | `sprite:alice_smile:left;` |
| `say` | 角色对话 | `say:爱丽丝:你好！;` |
| `bgm` | 播放背景音乐 | `bgm:quiet_theme;` |
| `sfx` | 播放音效 | `sfx:door.wav;` |
| `choose` | 选项分支 | `choose:进教室:scene_002 | 离开:scene_003;` |
| `var` | 设置变量 | `var:affection+1;` |
| `goto` | 跳转场景 | `goto:scene_002;` |

---

*本需求文档基于对 ToAVGAcme、Tuesday JS、WebGAL、VNgine、GinkgoEngine、StoryWeaver、Twine 等同类项目的调研整理，将随开发进展持续迭代。*
